<?php

namespace App\Database\Seeds;

use CodeIgniter\Database\Seeder;

/**
 * Seeder untuk kategori artikel tambahan (opsional).
 * Tidak dipanggil dari DatabaseSeeder — kategori default
 * sudah di-seed oleh MenuSeeder.
 */
class KategoriArtikelSeeder extends Seeder
{
    public function run(): void
    {
        // Kategori default sudah di-seed oleh MenuSeeder.
        // File ini disimpan untuk keperluan pengembangan manual.
    }
}
